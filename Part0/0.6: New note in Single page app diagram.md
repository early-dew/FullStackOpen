```mermaid
sequenceDiagram
 participant browser
 participant server
 browser->>server: POST to the server address new_note_spa
 server-->>browser: Sends JSON file (status code 201 created)
 deactivate server

```
